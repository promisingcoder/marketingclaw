interface:
  display_name: "MarketingClaw Release CI"
  short_description: "Verify and debug MarketingClaw release validation runs"
  default_prompt: "Use $release-marketingclaw-ci to preflight provider secrets, watch full release validation, summarize child runs, and triage only failing release lanes."
