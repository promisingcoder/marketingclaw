apiVersion: v1
kind: Service
metadata:
  name: marketingclaw
  labels:
    app: marketingclaw
spec:
  type: ClusterIP
  selector:
    app: marketingclaw
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
